import React from 'react';
import styles from "./log.module.scss";
const log = (props) => {
    return (
        <div className={styles.log}>
            log
        </div>
    );
}

export default log;
